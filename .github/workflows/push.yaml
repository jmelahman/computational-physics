name: On Push
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Set up Python 3.7
        uses: actions/setup-python@v1
        with:
          python-version: 3.7
      - name: Install deps
        run: >-
          python -m pip install --upgrade pip &&
          pip3 install -r requirements.txt
          ln -s $(pwd) /usr/local/lib/pythonX.Y/site-packages
      - name: Run tests
        run: >-
          python -m unittest discover tests/
      - name: Lint python
        run: >-
          pylint --fail-under=4.46 **/*.py
